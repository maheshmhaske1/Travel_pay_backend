/**
 * node-disk-storage
 * @author Copyright(c) 2021 by Restu wahyu saputra
 * MIT Licensed
 */
(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.matchProperty = void 0;
    const matchProperty = (compare) => {
        const defaultProperty = { minSize: undefined, maxSize: undefined };
        const compareIn = Object.keys(compare);
        const newInData = compareIn.map((v) => `${v}` in defaultProperty);
        return newInData.includes(false) ? undefined : true;
    };
    exports.matchProperty = matchProperty;
});
